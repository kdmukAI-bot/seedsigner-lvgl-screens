# custom-c-modules

Custom C modules and bindings intended for MicroPython/ESP-IDF integration.

This directory is structured to be split into a standalone GitHub repository.

## Active implementation tracker

- Input navigation + desktop runner plan:
  - `docs/INPUT_NAVIGATION_IMPLEMENTATION_PLAN.md`

## Tools

- Screenshot generator: `tools/screenshot_generator/`
- Screen runner (scaffold): `tools/screen_runner/`
